import React, { FC } from 'react';

import Link from 'next/link'


import styles from '../styles/Home.module.css'

type SponsorListProps = {
  // headerImgClass: string,
  }
  
  export const SponsorList: FC<SponsorListProps & React.HTMLAttributes<HTMLDivElement>> = ({  children }) => {
    return (
      
      // This component has not been populated because I'm not sure we'll need it in the first place, but it's here just in case - Victoire


      
    );
  }